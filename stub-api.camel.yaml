- beans:
    - name: fakerInstance
      type: com.github.javafaker.Faker
- route:
    id: route-2971
    from:
      id: from-3161
      uri: timer
      parameters:
        period: "1000"
        timerName: template
      steps:
        - bean:
            id: bean-3475
            method: address
            ref: fakerInstance
        - log:
            id: log-2841
            message: ${body.streetName()} ${body.buildingNumber()}
